# Optional in production if frontend and backend share same origin.
# For local dev, you can leave VITE_API_BASE_URL empty and rely on Vite proxy.
VITE_API_BASE_URL=
VITE_DEV_BACKEND_URL=http://localhost:8000
